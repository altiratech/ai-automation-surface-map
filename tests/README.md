# Tests

Prioritize:
- step/schema validation
- scoring contract tests
- one workflow-to-ranked-output integration test
- one cross-slice synthesis check over the committed locked workflow artifacts
- one reviewer-workbench slice check over the shared assist-lane steps
